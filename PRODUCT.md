# PRODUCT

## 目的
市民マラソン向けの Connect IQ データフィールド（1画面）を実装し、数値を次の行動に変換する「リアルタイム伴走」を提供する。

- 主対象: PB 5:00前後のランナー
- レース中の判断負荷を減らし、オーバーペースによる失速リスクを下げる

## Core Runner Value
- ペース迷子を防ぐ
- 失速リスクを下げる
- 崩れの兆候に先回りする
- メンタル負荷を下げる

## 現在スコープ
- 当面は通常版（Core）の完成を最優先
- 通常版に加えて、必要時のみカスタムコード入力を使える
- 後半レースモードは実装しない

## 公開版で含めないもの
- FIT自動取り込みとレース後の詳細分析
- 公開版UIでの詳細パラメタ編集
- 個人データの解釈を前提とした個別最適化

上記は通常版には含めず、必要なら `LTHR` のプロパティ指定またはカスタムコード運用で扱う。

## 成果物
- Connect IQ Data Field（1面）
- 心拍CAPダッシュボード
- 予測ゴール時刻と差分
- ゴールランナーゲージ
- 距離/経過時間表示

## 設定（通常版）
- `raceDistanceKm`（default: 42.195）
- `targetTimeSec`（default: 05:00:00）
- `lthr_bpm`（未指定なら端末/デバイス値へフォールバック）
- `custom_mode_code`（UI表示名は Custom Code）

`custom_mode_code` は内部キー名として残すが、ユーザー向け名称は「カスタムコード」に統一する。

## 機能提案の優先基準
- データ表示より「次の行動提案」への変換を優先
- 走行中に迷わない短文・即時判断を優先
- 後半失速防止（ペース/心拍）への寄与を明示
- 完走確率向上または目標達成確率向上への効果を説明
