# PRODUCT

## 位置づけ
- このリポジトリの通常版プロダクトは `MarathonCoach / RaceNavi Core`。
- 通常版の機能仕様は `docs/spec/core.md` を正とする。
- カスタムコード仕様は `docs/spec/custom_code.md` を正とする。
- GateChecker は別系統のアプリであり、現行方針は `apps/GateChecker/README.md` の単一アプリ + Race Code 方式を正とする。

## 目的
市民マラソン向けの Connect IQ Data Field を実装し、レース中に一瞬で読めるダッシュボードで「判断の迷いを減らす」ことを主価値にする。

## 対象
- 主対象: PB 5:00前後の市民ランナー
- 相性が良い層: 完走狙いからサブ4前後までの、ペース配分と心拍管理に不安があるランナー

## Core Runner Value
- ペース迷子を防ぐ
- 失速リスクを下げる
- 心拍上限を守りやすくする
- ゴール予測を見ながら落ち着いて判断できるようにする
- レース中のメンタル負荷を下げる

## 現在スコープ
- 当面は通常版（Core）の完成と公開品質向上を最優先にする
- 画面は `docs/spec/core.md` にある常時表示ダッシュボードを維持する
- 通常版に加えて、必要時のみ `custom_mode_code` を使ったカスタムコード運用を許容する
- 後半レース専用の別モードや、画面切り替え前提の複雑なUXは持たない

## 公開版で含めないもの
- FIT 自動取り込みとレース後の詳細分析
- 公開版UIでの詳細パラメタ編集
- 個人データの解釈を前提とした個別最適化
- 独自距離通知カード、常時ACTION表示、コーチカード常設

上記は通常版には含めず、必要なら `lthr_bpm` またはカスタムコード運用で扱う。

## 設定と詳細仕様
- 通常版の設定キー名、入力範囲、表示仕様は `docs/spec/core.md` を参照する。
- `custom_mode_code` は内部キー名として残すが、ユーザー向け名称は「カスタムコード」に統一する。

## 機能提案の優先基準
- データ量よりも「次の判断を早くする」ことを優先する
- 走行中に迷わない短文・即時判断を優先する
- 後半失速防止への寄与を、ペースと心拍の両面で説明できるものを優先する
- 公開版の複雑さを増やさず、1画面ダッシュボードの価値を強めるものを優先する
