# 2-2. 異常系確認

- Source: `TASKS.md`

## T01 [P0] 心拍が取れないケースの確認

- タスク内容:
  - 心拍が取れないケースの確認
- 目的:
  - 公開前に実機運用で再現できる品質判定結果を残すため。
- 草案:
  - 検証対象: 心拍が取れないケースの確認。
  - テストケース: 心拍値null時にクラッシュしない / 心拍依存通知を停止し安全表示に切り替わる / 心拍復帰後に通常表示へ戻る。
  - 結果記録フォーマット: `ケース名 | 期待結果 | 実測結果 | 判定(OK/NG) | 再現手順 | ログ/動画パス`。
  - 合格基準: P0ケースが全てOK、P1ケースは既知問題として回避策を明記。
  - 注意点: 手順だけでなく、期待値と実測値を同じ行に書き、第三者が追試できる証跡にする。
- 結果:
  - TBD

## T02 [P0] GPSが荒れるケースの確認

- タスク内容:
  - GPSが荒れるケースの確認
- 目的:
  - 公開前に実機運用で再現できる品質判定結果を残すため。
- 草案:
  - 検証対象: GPSが荒れるケースの確認。
  - テストケース: 距離ジャンプ発生時に異常通知を抑止する / ペース急変時に直前安定値へフォールバックする / GPS復帰後に通常計算へ戻る。
  - 結果記録フォーマット: `ケース名 | 期待結果 | 実測結果 | 判定(OK/NG) | 再現手順 | ログ/動画パス`。
  - 合格基準: P0ケースが全てOK、P1ケースは既知問題として回避策を明記。
  - 注意点: 手順だけでなく、期待値と実測値を同じ行に書き、第三者が追試できる証跡にする。
- 結果:
  - TBD

## T03 [P0] 設定未入力・異常値入力の確認

- タスク内容:
  - 設定未入力・異常値入力の確認
- 目的:
  - 公開前に実機運用で再現できる品質判定結果を残すため。
- 草案:
  - 検証対象: 設定未入力・異常値入力の確認。
  - テストケース: 未入力時に初期値で起動できる / 異常値入力を保存できない / エラーメッセージが1回で理解できる。
  - 結果記録フォーマット: `ケース名 | 期待結果 | 実測結果 | 判定(OK/NG) | 再現手順 | ログ/動画パス`。
  - 合格基準: P0ケースが全てOK、P1ケースは既知問題として回避策を明記。
  - 注意点: 手順だけでなく、期待値と実測値を同じ行に書き、第三者が追試できる証跡にする。
- 結果:
  - TBD

## T04 [P1] 通知が多すぎるケースの確認

- タスク内容:
  - 通知が多すぎるケースの確認
- 目的:
  - 公開前に実機運用で再現できる品質判定結果を残すため。
- 草案:
  - 検証対象: 通知が多すぎるケースの確認。
  - テストケース: 同一種別通知のクールダウンが機能する / 1分以内の重複通知を抑止する / 重要通知のみ残して二次通知を間引く。
  - 結果記録フォーマット: `ケース名 | 期待結果 | 実測結果 | 判定(OK/NG) | 再現手順 | ログ/動画パス`。
  - 合格基準: P0ケースが全てOK、P1ケースは既知問題として回避策を明記。
  - 注意点: 手順だけでなく、期待値と実測値を同じ行に書き、第三者が追試できる証跡にする。
- 結果:
  - TBD

## 追加改善（マーケ視点）

- T01: 心拍欠損ケースに `初心者が不安にならない代替表示文言` を固定し、体験離脱を防ぐ。
- T02: GPS荒れケースは `トンネル/高層街/悪天候` の具体シーン名で記録し、訴求素材として再利用する。
- T03: 入力異常時は `次に何を直せば良いか` を1文表示する基準を追加する。
- T04: 通知過多検証に `主観疲労スコア(5段階)` を入れ、数字で最適化できるようにする。

## 【斬新案】

- `逆境テスト配信`:
  - あえてGPSが荒れる環境で検証し、「崩れない設計」を短編動画で公開する。
